import { join } from "path";

export const icons = join(__dirname, "../../resources/icons");

export const config = join(__dirname, "../../resources/config");

export const iconsDirPath = join(__dirname, "../../resources/icons");
